apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: federated-server
  name: prometheus-federated
  namespace: monitoring
spec:
  ports:
  - name: web
    port: 9090
    targetPort: web
  selector:
    app: prometheus
    prometheus: federated
